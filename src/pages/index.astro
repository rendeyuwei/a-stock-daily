---
---
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>A 股交易日报</title>
    <style>
        :root {
            --primary: #6366f1;
            --primary-dark: #4f46e5;
            --success: #10b981;
            --danger: #ef4444;
            --bg-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            --card-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            --card-hover: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
        }
        
        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        body { 
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            background: var(--bg-gradient);
            min-height: 100vh;
            padding: 20px;
            line-height: 1.6;
        }
        
        .container { 
            max-width: 1400px; 
            margin: 0 auto; 
            background: rgba(255, 255, 255, 0.98);
            border-radius: 24px;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
            overflow: hidden;
            backdrop-filter: blur(10px);
        }
        
        .header {
            background: linear-gradient(135deg, #1e293b 0%, #3b82f6 100%);
            color: white;
            padding: 40px 30px;
            text-align: center;
            position: relative;
            overflow: hidden;
        }
        
        .header::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, transparent 70%);
            animation: shimmer 3s infinite;
        }
        
        @keyframes shimmer {
            0%, 100% { transform: translate(-20%, -20%); }
            50% { transform: translate(20%, 20%); }
        }
        
        .header h1 { 
            font-size: 2.5em; 
            margin-bottom: 10px;
            font-weight: 700;
            position: relative;
            z-index: 1;
        }
        
        .header p { 
            opacity: 0.9; 
            font-size: 1.1em;
            position: relative;
            z-index: 1;
        }
        
        .update-time {
            background: rgba(255,255,255,0.15);
            padding: 10px 20px;
            border-radius: 24px;
            display: inline-block;
            margin-top: 20px;
            font-size: 0.95em;
            backdrop-filter: blur(10px);
            position: relative;
            z-index: 1;
            transition: transform 0.2s;
        }
        
        .update-time:hover {
            transform: scale(1.05);
        }
        
        .content { padding: 40px; }
        
        .section { margin-bottom: 40px; }
        
        .section-title {
            font-size: 1.6em;
            color: #1e293b;
            border-left: 5px solid var(--primary);
            padding-left: 20px;
            margin-bottom: 25px;
            font-weight: 700;
        }
        
        /* 策略卡片 */
        .strategy-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }
        
        .strategy-card {
            background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
            border-radius: 16px;
            padding: 25px;
            border: 1px solid rgba(99, 102, 241, 0.1);
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }
        
        .strategy-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 4px;
            height: 100%;
            background: var(--primary);
        }
        
        .strategy-card:hover {
            transform: translateY(-5px);
            box-shadow: var(--card-hover);
        }
        
        .strategy-card h4 {
            color: var(--primary-dark);
            font-size: 1.1em;
            margin-bottom: 10px;
            display: flex;
            align-items: center;
            gap: 8px;
        }
        
        .strategy-card p {
            color: #64748b;
            font-size: 0.95em;
        }
        
        /* 股票卡片网格 */
        .stocks-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
            gap: 20px;
        }
        
        .stock-card {
            background: white;
            border-radius: 16px;
            padding: 24px;
            box-shadow: var(--card-shadow);
            transition: all 0.3s ease;
            border: 1px solid #e2e8f0;
            position: relative;
            overflow: hidden;
        }
        
        .stock-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, var(--primary), var(--primary-dark));
        }
        
        .stock-card:hover {
            transform: translateY(-8px);
            box-shadow: var(--card-hover);
        }
        
        .stock-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 16px;
            padding-bottom: 16px;
            border-bottom: 1px solid #f1f5f9;
        }
        
        .stock-code {
            font-family: "SF Mono", "Monaco", "Inconsolata", monospace;
            font-weight: 700;
            font-size: 1.3em;
            color: #1e293b;
            background: linear-gradient(135deg, #f1f5f9 0%, #e2e8f0 100%);
            padding: 8px 14px;
            border-radius: 10px;
        }
        
        .stock-name {
            font-weight: 600;
            font-size: 1.1em;
            color: #334155;
        }
        
        .stock-change {
            font-weight: 700;
            font-size: 1.2em;
            padding: 6px 12px;
            border-radius: 8px;
            display: inline-block;
        }
        
        .stock-change.positive {
            color: #dc2626;
            background: rgba(220, 38, 38, 0.1);
        }
        
        .stock-change.negative {
            color: #16a34a;
            background: rgba(22, 163, 74, 0.1);
        }
        
        .stock-body {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 12px;
            margin-bottom: 16px;
        }
        
        .stock-stat {
            background: #f8fafc;
            padding: 12px;
            border-radius: 10px;
        }
        
        .stock-stat-label {
            font-size: 0.8em;
            color: #94a3b8;
            margin-bottom: 4px;
        }
        
        .stock-stat-value {
            font-weight: 600;
            color: #334155;
            font-size: 1.05em;
        }
        
        .stock-reason {
            background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%);
            padding: 14px;
            border-radius: 10px;
            border-left: 3px solid #f59e0b;
        }
        
        .stock-reason-label {
            font-size: 0.8em;
            color: #92400e;
            margin-bottom: 6px;
            font-weight: 600;
        }
        
        .stock-reason-value {
            color: #78350f;
            font-weight: 500;
            font-size: 0.95em;
        }
        
        .stats-bar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px;
            background: linear-gradient(135deg, #f1f5f9 0%, #e2e8f0 100%);
            border-radius: 12px;
            margin-bottom: 25px;
        }
        
        .stats-count {
            font-size: 1.1em;
            color: #475569;
        }
        
        .stats-count strong {
            color: var(--primary);
            font-size: 1.4em;
        }
        
        .footer {
            text-align: center;
            padding: 30px;
            background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
            color: #64748b;
            font-size: 0.9em;
            border-top: 1px solid #e2e8f0;
        }
        
        .footer p { margin: 5px 0; }
        
        @media (max-width: 768px) {
            .header h1 { font-size: 1.8em; }
            .content { padding: 20px; }
            .stocks-grid { grid-template-columns: 1fr; }
            .strategy-grid { grid-template-columns: 1fr; }
            .stock-body { grid-template-columns: 1fr; }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>📈 A 股交易日报</h1>
            <p>系统化交易 · 智能选股 · 每日更新</p>
            <div class="update-time" id="updateTime">📅 更新日期：加载中...</div>
        </div>
        
        <div class="content">
            <div class="section">
                <h2 class="section-title">📋 策略核心</h2>
                <div class="strategy-grid">
                    <div class="strategy-card">
                        <h4>📊 基本面</h4>
                        <p>ROE > 15%，营收增长 > 20%</p>
                    </div>
                    <div class="strategy-card">
                        <h4>📈 技术面</h4>
                        <p>均线多头排列，MACD 金叉</p>
                    </div>
                    <div class="strategy-card">
                        <h4>🚀 催化剂</h4>
                        <p>业绩/合同/政策驱动</p>
                    </div>
                    <div class="strategy-card">
                        <h4>🛡️ 止损</h4>
                        <p>跌破成本 5-8% 或 20 日均线</p>
                    </div>
                    <div class="strategy-card">
                        <h4>💰 止盈</h4>
                        <p>盈利 10-15% 分批卖出</p>
                    </div>
                </div>
            </div>
            
            <div class="section">
                <h2 class="section-title">🎯 候选股票</h2>
                <div id="stockData" style="text-align: center; padding: 60px 20px; color: #94a3b8;">
                    <div style="font-size: 3em; margin-bottom: 20px;">⏳</div>
                    <p style="font-size: 1.1em;">正在加载今日数据...</p>
                </div>
            </div>
        </div>
        
        <div class="footer">
            <p>自动生成 by 小豆 · 仅供研究参考，不构成投资建议</p>
            <p>⚠️ 股市有风险，投资需谨慎</p>
        </div>
    </div>
    
    <script>
        // 更新日期
        const today = new Date();
        document.getElementById('updateTime').textContent = '📅 更新日期：' + today.toLocaleDateString('zh-CN');
        
        // 加载股票数据
        async function loadStockData() {
            try {
                const response = await fetch('/data/stocks.json');
                const data = await response.json();
                renderStocks(data);
            } catch (error) {
                document.getElementById('stockData').innerHTML = '加载失败，请稍后刷新';
            }
        }
        
        function renderStocks(data) {
            const stocks = data.stocks || [];
            const date = data.date || '今日';
            
            document.getElementById('updateTime').textContent = '📅 数据日期：' + date;
            
            let html = `
                <div class="stats-bar">
                    <div class="stats-count">共 <strong>${stocks.length}</strong> 只候选股票</div>
                    <div style="color: #64748b; font-size: 0.9em;">数据日期：${date}</div>
                </div>
                <div class="stocks-grid">
            `;
            
            stocks.forEach(stock => {
                const changeClass = stock.change.includes('+') || !stock.change.includes('-') ? 'positive' : 'negative';
                const changeSign = stock.change.includes('-') ? '' : '+';
                html += `
                    <div class="stock-card">
                        <div class="stock-header">
                            <div>
                                <div class="stock-code">${stock.code}</div>
                                <div class="stock-name" style="margin-top: 8px;">${stock.name}</div>
                            </div>
                            <div class="stock-change ${changeClass}">${changeSign}${stock.change}%</div>
                        </div>
                        <div class="stock-body">
                            <div class="stock-stat">
                                <div class="stock-stat-label">当前价格</div>
                                <div class="stock-stat-value">¥${stock.price}</div>
                            </div>
                            <div class="stock-stat">
                                <div class="stock-stat-label">成交量 (万手)</div>
                                <div class="stock-stat-value">${stock.volume}</div>
                            </div>
                        </div>
                        <div class="stock-reason">
                            <div class="stock-reason-label">🎯 入选理由</div>
                            <div class="stock-reason-value">${stock.reason || '综合评分优异'}</div>
                        </div>
                    </div>
                `;
            });
            
            html += '</div>';
            document.getElementById('stockData').innerHTML = html;
        }
        
        loadStockData();
    </script>
</body>
</html>
