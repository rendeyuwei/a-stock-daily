---
---
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>A 股交易日报</title>
    <style>
        :root {
            --primary: #6366f1;
            --primary-dark: #4f46e5;
            --up: #ef4444;
            --down: #22c55e;
            --bg-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }
        
        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        body { 
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            background: var(--bg-gradient);
            min-height: 100vh;
            padding: 15px;
        }
        
        .container { 
            max-width: 1200px; 
            margin: 0 auto; 
            background: white;
            border-radius: 16px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.25);
            overflow: hidden;
        }
        
        .header {
            background: linear-gradient(135deg, #1e293b 0%, #3b82f6 100%);
            color: white;
            padding: 25px 20px;
            text-align: center;
        }
        
        .header h1 { font-size: 1.8em; font-weight: 700; margin-bottom: 8px; }
        .header p { opacity: 0.9; font-size: 0.95em; }
        
        .update-time {
            background: rgba(255,255,255,0.15);
            padding: 6px 14px;
            border-radius: 20px;
            display: inline-block;
            margin-top: 12px;
            font-size: 0.85em;
        }
        
        .content { padding: 20px; }
        
        .section { margin-bottom: 30px; }
        
        .section-title {
            font-size: 1.3em;
            color: #1e293b;
            border-left: 4px solid var(--primary);
            padding-left: 12px;
            margin-bottom: 15px;
            font-weight: 600;
        }
        
        /* 策略标签 - 简化展示 */
        .strategy-tags {
            display: flex;
            flex-wrap: wrap;
            gap: 8px;
        }
        
        .strategy-tag {
            background: linear-gradient(135deg, #f1f5f9 0%, #e2e8f0 100%);
            padding: 8px 14px;
            border-radius: 8px;
            font-size: 0.85em;
            color: #475569;
            border: 1px solid #cbd5e1;
        }
        
        .strategy-tag strong {
            color: var(--primary-dark);
        }
        
        /* 股票表格 */
        .table-container {
            overflow-x: auto;
            -webkit-overflow-scrolling: touch;
            border-radius: 12px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.08);
        }
        
        .stock-table {
            width: 100%;
            border-collapse: collapse;
            background: white;
            min-width: 700px;
        }
        
        .stock-table th {
            background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
            color: #475569;
            padding: 14px 12px;
            text-align: left;
            font-weight: 600;
            font-size: 0.85em;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            border-bottom: 2px solid #e2e8f0;
            white-space: nowrap;
        }
        
        .stock-table td {
            padding: 14px 12px;
            border-bottom: 1px solid #f1f5f9;
            font-size: 0.9em;
        }
        
        .stock-table tr {
            transition: background 0.2s;
        }
        
        .stock-table tr:hover {
            background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%);
        }
        
        .stock-table tr:last-child td {
            border-bottom: none;
        }
        
        .stock-code { 
            font-family: "SF Mono", "Monaco", monospace; 
            font-weight: 700;
            color: #1e293b;
            background: #f1f5f9;
            padding: 4px 10px;
            border-radius: 6px;
            font-size: 0.95em;
        }
        
        .stock-name { 
            font-weight: 600; 
            color: #334155;
        }
        
        .stock-price {
            font-weight: 600;
            color: #1e293b;
        }
        
        .stock-change {
            font-weight: 700;
            padding: 4px 10px;
            border-radius: 6px;
            display: inline-block;
            min-width: 60px;
            text-align: center;
        }
        
        .stock-change.up {
            color: #dc2626;
            background: rgba(220, 38, 38, 0.1);
        }
        
        .stock-change.down {
            color: #16a34a;
            background: rgba(22, 163, 74, 0.1);
        }
        
        .stock-volume {
            color: #64748b;
            font-weight: 500;
        }
        
        .stock-reason {
            color: #78350f;
            background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%);
            padding: 6px 10px;
            border-radius: 6px;
            border-left: 3px solid #f59e0b;
            font-size: 0.85em;
        }
        
        .stats-bar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 12px 16px;
            background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
            border-radius: 10px;
            margin-bottom: 15px;
            border: 1px solid #e2e8f0;
        }
        
        .stats-count {
            font-size: 0.95em;
            color: #475569;
        }
        
        .stats-count strong {
            color: var(--primary);
            font-size: 1.2em;
        }
        
        .footer {
            text-align: center;
            padding: 20px;
            background: #f8fafc;
            color: #94a3b8;
            font-size: 0.85em;
            border-top: 1px solid #e2e8f0;
        }
        
        .footer p { margin: 4px 0; }
        
        @media (max-width: 768px) {
            body { padding: 10px; }
            .header h1 { font-size: 1.4em; }
            .header p { font-size: 0.85em; }
            .content { padding: 15px; }
            .section-title { font-size: 1.1em; }
            .strategy-tags { gap: 6px; }
            .strategy-tag { font-size: 0.75em; padding: 6px 10px; }
            .stock-table td, .stock-table th { padding: 10px 8px; }
            .stock-code { font-size: 0.85em; padding: 3px 8px; }
            .stock-change { min-width: 50px; font-size: 0.85em; }
            .stock-reason { font-size: 0.8em; }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>📈 A 股交易日报</h1>
            <p>系统化交易 · 智能选股 · 每日更新</p>
            <div class="update-time" id="updateTime">📅 更新日期：加载中...</div>
        </div>
        
        <div class="content">
            <div class="section">
                <h2 class="section-title">📋 策略核心</h2>
                <div class="strategy-tags">
                    <div class="strategy-tag">📊 基本面 <strong>ROE&gt;15%</strong> 营收&gt;20%</div>
                    <div class="strategy-tag">📈 技术面 <strong>均线多头</strong> MACD 金叉</div>
                    <div class="strategy-tag">🚀 催化剂 <strong>业绩/合同/政策</strong></div>
                    <div class="strategy-tag">🛡️ 止损 <strong>-5~-8%</strong> 或 20 日线</div>
                    <div class="strategy-tag">💰 止盈 <strong>+10~15%</strong> 分批</div>
                </div>
            </div>
            
            <div class="section">
                <h2 class="section-title">🎯 候选股票</h2>
                <div id="stockData" style="text-align: center; padding: 50px 20px; color: #94a3b8;">
                    <div style="font-size: 2.5em; margin-bottom: 15px;">⏳</div>
                    <p>正在加载今日数据...</p>
                </div>
            </div>
        </div>
        
        <div class="footer">
            <p>自动生成 by 小豆 · 仅供研究参考，不构成投资建议</p>
            <p>⚠️ 股市有风险，投资需谨慎</p>
        </div>
    </div>
    
    <script>
        // 更新日期
        const today = new Date();
        document.getElementById('updateTime').textContent = '📅 更新日期：' + today.toLocaleDateString('zh-CN');
        
        // 加载股票数据
        async function loadStockData() {
            try {
                const response = await fetch('/data/stocks.json');
                const data = await response.json();
                renderStocks(data);
            } catch (error) {
                document.getElementById('stockData').innerHTML = '加载失败，请稍后刷新';
            }
        }
        
        function renderStocks(data) {
            const stocks = data.stocks || [];
            const date = data.date || '今日';
            
            document.getElementById('updateTime').textContent = '📅 数据日期：' + date;
            
            let html = `
                <div class="stats-bar">
                    <div class="stats-count">共 <strong>${stocks.length}</strong> 只候选股票</div>
                    <div style="color: #64748b; font-size: 0.85em;">${date}</div>
                </div>
                <div class="table-container">
                    <table class="stock-table">
                        <thead>
                            <tr>
                                <th>代码</th>
                                <th>名称</th>
                                <th>价格</th>
                                <th>涨跌幅</th>
                                <th>成交量</th>
                                <th>入选理由</th>
                            </tr>
                        </thead>
                        <tbody>
            `;
            
            stocks.forEach(stock => {
                const changeSign = stock.change.includes('-') ? '' : '+';
                const changeClass = stock.change.includes('-') ? 'down' : 'up';
                html += `
                    <tr>
                        <td><span class="stock-code">${stock.code}</span></td>
                        <td><span class="stock-name">${stock.name}</span></td>
                        <td class="stock-price">¥${stock.price}</td>
                        <td><span class="stock-change ${changeClass}">${changeSign}${stock.change}%</span></td>
                        <td class="stock-volume">${stock.volume}万手</td>
                        <td><span class="stock-reason">${stock.reason || '综合评分优异'}</span></td>
                    </tr>
                `;
            });
            
            html += '</tbody></table></div>';
            document.getElementById('stockData').innerHTML = html;
        }
        
        loadStockData();
    </script>
</body>
</html>
